/**
 * Free-form dictation for a new todo. Matches the `title` slot of the
 * `todo.create` intent in src/shared/voice/catalog.ts.
 */
text (TodoTitle) {
  description (What the user wants to be reminded of.)
}
